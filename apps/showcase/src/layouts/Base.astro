---
import { noFlashScript } from '../../../../packages/theme-core/src/helper';
import '../../../../packages/theme-core/styles/tokens.css';
import '../styles/showcase.css';

import Header from '../../../../packages/easy-web-content-blocks/src/components/Header.astro';
import Footer from '../../../../packages/easy-web-content-blocks/src/components/Footer.astro';
import ThemeToggle from '../../../../packages/easy-web-content-blocks/src/components/ThemeToggle.astro';

interface Props {
  title: string;
  description?: string;
  lang?: string;
}

const { title, description, lang = 'en' } = Astro.props;

const currentLang = lang;
const pathname = Astro.url.pathname;
const isDe = currentLang === 'de';

const navItems = [
  { label: isDe ? 'Startseite' : 'Home', href: `${import.meta.env.BASE_URL}` },
  { label: 'Themes', href: `${import.meta.env.BASE_URL}themes/` },
  { label: isDe ? 'Komponenten' : 'Components', href: `${import.meta.env.BASE_URL}components/` },
  { label: 'Guide', href: `${import.meta.env.BASE_URL}guide/` },
  { label: 'Docs', href: `${import.meta.env.BASE_URL}docs/` },
];

const legalLinks = [
  { label: isDe ? 'Impressum' : 'Imprint', href: `${import.meta.env.BASE_URL}imprint/` },
  { label: isDe ? 'Datenschutz' : 'Privacy', href: `${import.meta.env.BASE_URL}privacy/` }
];
---
<!doctype html>
<html lang={lang}>
  <head>
    <script is:inline set:html={noFlashScript}></script>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    {description && <meta name="description" content={description} />}
    <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}favicon.svg`} />
  </head>
  <body>
    <Header
      siteName="easy-web"
      navItems={navItems}
      currentLang={currentLang}
      pathname={pathname}
    />
    <main>
      <slot />
    </main>
    <Footer siteName="easy-web" legalLinks={legalLinks} />
  </body>
</html>
